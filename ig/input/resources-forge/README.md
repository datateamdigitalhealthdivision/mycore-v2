# Forge-Owned Resources

This directory is human-owned and never wiped by the build.

Use `resources-forge/` for Forge-authored `StructureDefinition`, extension, `SearchParameter`, `Questionnaire`, and other conformance resources that the MY Core team actively maintains.

Do not place international terminology content here.
